<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.food_rec.mapper.UserMapper">
    <select id="selectUserById" resultType="com.example.food_rec.domain.User" parameterType="String">
        SELECT * from user where id = #{uid}
    </select>
</mapper>